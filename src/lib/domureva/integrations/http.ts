export async function signedPost(base:string|undefined,path:string,token:string|undefined,payload:unknown){if(!base)throw new Error('Integration base URL missing');const r=await fetch(new URL(path,base),{method:'POST',headers:{'content-type':'application/json',...(token?{authorization:`Bearer ${token}`}:{})},body:JSON.stringify(payload)});if(!r.ok)throw new Error(`Integration ${r.status}`);return r.json();}
